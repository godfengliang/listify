[tool.poetry]
name = "listify"
version = "0.1.0"
description = "AI Multi-Platform Listing Generator for E-Commerce Sellers"
authors = ["fenley"]

[tool.poetry.dependencies]
python = "^3.11"

[build-system]
requires = ["poetry-core"]
